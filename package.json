{
  "name": "focairemover",
  "private": true,
  "version": "0.0.0",
  "description": "Cloudflare-hosted AI provenance cleaner. Planning/MVP scaffold — see docs/PLAN.md.",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
